server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri: git@github.com:yjl-001/configuration.git
          default-label: main
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka/